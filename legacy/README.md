# Legacy

Notebooks e saidas da fase inicial do projeto, antes do pacote `flatfurious/`.

Nao use estes arquivos no fluxo automatizado. Consulte apenas para referencia.

- `notebooks/` — Jupyter duplicados que estavam na raiz do repo
- `outputs/` — PNGs antigos (pasta `out/` original)
